---
report-id: RG-V423-20260922-03
role: github
reviewer: github
mode: RELEASE_GATE
version: 4.2.3
package_fingerprint: 7bc2792c7e876cb2a47d1b17ff09c5d90f8e58bfda763779026b85885dec8809
base_ref: v4.2.2
target_ref: WORKTREE:7bc2792c7e876cb2a47d1b17ff09c5d90f8e58bfda763779026b85885dec8809
changed_files: [".githooks/pre-push", ".gitignore", ".zcode-plugin/plugin.json", "CHANGELOG.md", "INSTALL-FOR-AI.md", "README.md", "scripts/manage.py", "scripts/model_inventory.py", "scripts/release_gate.py", "tests/fixtures/provider_config.v3.12.3.sanitized.json", "tests/test_manage.py", "tests/test_model_inventory.py", "tests/test_release_gate.py"]
removed_files: []
changed_agents: []
breaking_impact: breaking
reviewed_at: 2026-09-22T10:15:00Z
verdict: PASS
---

# Release Gate v4.2.3

## Scope
- changed_files: `.githooks/pre-push`
- changed_files: `.gitignore`
- changed_files: `.zcode-plugin/plugin.json`
- changed_files: `CHANGELOG.md`
- changed_files: `INSTALL-FOR-AI.md`
- changed_files: `README.md`
- changed_files: `scripts/manage.py`
- changed_files: `scripts/model_inventory.py`
- changed_files: `scripts/release_gate.py`
- changed_files: `tests/fixtures/provider_config.v3.12.3.sanitized.json`
- changed_files: `tests/test_manage.py`
- changed_files: `tests/test_model_inventory.py`
- changed_files: `tests/test_release_gate.py`
- removed_files: none
- changed_agents: none

## Evidence
| evidence-id | check | result | evidence |
|---|---|---|---|
| EV-01 | validation | PASS | 主控真实执行 `python3 scripts/manage.py validate` 通过（`VALIDATION OK: 22 agents, plugin tony-agents-pack v4.2.3`）；只读复核确认 `scripts/manage.py` 第 31 行 `EXPECTED_AGENT_COUNT = 22`，`agents/` 目录恰为 22 个 `*.md` 文本契约，`.zcode-plugin/plugin.json` 为 `name=tony-agents-pack`、`version=4.2.3`、`agents=agents`。 |
| EV-02 | tests | PASS | 主控真实执行 `python3 -m unittest discover -s tests -p 'test_*.py' -v` 通过（`Ran 219 tests ... OK (skipped=1)`，唯一 skip 为维护者专用本地同步检查）；只读统计用例数：`test_manage.py` 168 项、`test_release_gate.py` 31 项、`test_model_inventory.py` 20 项，合计 219 项，用例数与执行结果精确相符。所有脚本与测试均通过 `py_compile`，`git diff --check` 无空白与语法异常。 |
| EV-03 | fingerprint | PASS | 目标工作树 package fingerprint 经 Git index 算法计算为 `7bc2792c7e876cb2a47d1b17ff09c5d90f8e58bfda763779026b85885dec8809`，与 `target_ref` 后缀精确一致。所有 13 个发布 payload 路径均已 staged，未暂存与未跟踪发布载荷为空；版本审计报告（包含当前 `release-audits/v4.2.3.md` 及历史版本审计）按契约排除于 fingerprint 之外。 |
| EV-04 | scope-and-diff | PASS | 相对 `base_ref=v4.2.2` 的真实 staged 差异中，发布 payload 路径（按契约排除 `^release-audits/v\d+\.\d+\.\d+\.md$` 版本审计报告后）精确为 13 个文件，与 `changed_files` 逐项一致；无删除项（`removed_files: []`）；不含任何 `agents/*.md`（`changed_agents: []`）。22 个 agent 契约与 frontmatter 保持零改动。 |
| EV-05 | dual-track-implementation-review | PASS | 基于固定只读快照（manifest SHA `366b3101ddbb2269df1de910b07985d6d5979936c45a45c3d388b4e9ffe01dee`，patch SHA `640e082879d7308b165d1027bcccadc6aad2979453f4def51334b21094a0280f`）完成独立双路审查：Terra 通道（gpt-5.6-terra）PASS（10/10，P0=0/P1=0/P2=0/P3=0）；Flash 通道（deepseek-v4-flash）PASS（9/10，P0=0/P1=0/P2=2/P3=7）。Flash 发现的 P2-1、P2-2、P3-1、P3-3、P3-4、P3-7 均已在此次 fingerprint 定稿前完成修复并补齐回归测试（总测试数由 214 增至 219），剩余 3 个 OPEN P3 为非阻断工程边界并在 Findings 中如实登记。 |
| EV-06 | version-consistency | PASS | 全链路版本号严格对齐至 `4.2.3`：`.zcode-plugin/plugin.json`（`4.2.3`）、`CHANGELOG.md` 顶层（`## [4.2.3] — 2026-09-22`）、`README.md`（version 徽章 `v4.2.3`、安装/更新/卸载主提示词与手工克隆脚本均指向 `v4.2.3`、保留 v4.2.2 费用/定位历史说明）、`INSTALL-FOR-AI.md`（目标版本 `v4.2.3`、`4.2.3`、`同为 4.2.3`）、`tests/test_manage.py`（版本断言 `4.2.3`）。历史版本号仅正当出现在历史节与对比叙述中。 |
| EV-07 | security-and-sanitization | PASS | 全局敏感模式扫描未命中任何真实密钥、token、私有凭据或机器绝对路径；命中仅为测试用例反向探针、安全校验关键字及脱敏测试夹具（`tests/fixtures/provider_config.v3.12.3.sanitized.json`）。`scripts/model_inventory.py` 严格限定输出白名单字段，彻底消除敏感配置外泄风险。 |
| EV-08 | windows-ci-backstop | PASS | 首轮 PR required check 触发后 ubuntu 与 macos 均全绿，Windows 真机环境精准识别出 4 处测试套件适配缺陷（短路径 8.3 注入匹配失效、reparse point 报错正则限定过窄、未知 ~user 路径平台语义差异），产品代码零缺陷；测试修复已完整合入当前 staged 树，最新 head 的 `validate (windows-latest, 3.9)` required check 将执行真机终验并强制兜底。 |
| EV-09 | push-hook-incident-fix | PASS | 独立只读复核事故处置与根因修复代码：`.githooks/pre-push` 在调用 validate、unittest、check 门禁步骤前，已显式执行 `unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE GIT_OBJECT_DIRECTORY GIT_ALTERNATE_OBJECT_DIRECTORIES GIT_COMMON_DIR GIT_GRAFT_FILE GIT_NAMESPACE GIT_INTERNAL_SUPER_PREFIX GIT_PREFIX` 清除全部 Git 钩子继承环境变量；`tests/test_release_gate.py` 的 `git()` helper 子进程亦显式过滤剔除所有 `GIT_*` 环境变量，构成双重纵深防护。经模拟钩子环境及真实测试套件全绿验证，彻底杜绝测试夹具提交打入宿主仓库及移动维护者分支的隐患。 |

## Findings
| finding-id | severity | status | summary |
|---|---|---|---|
| RV423-001 | P3 | OPEN | `scripts/manage.py:1695-1701` 的 `install --force` 对“已从包中删除但上一轮曾被已选”的 agent 仅在 state 中除名，而在目标目录留下未受管孤儿文件，与 update 的 `removed` 清理逻辑不一致；该场景无数据丢失（快照已备份），后续建议在 force 中对已除名 agent 补齐清理或显式警示。 |
| RV423-002 | P3 | OPEN | `scripts/manage.py:696-716` 中 `exclusive_rename` 成功后但在 `fsync_directory`/`unlink_path(claimed)` 失败的极窄窗口内，目标文件已为新内容而 `_active_writes` 尚未登记，触发自动回滚时会跳过该路径；旧前像仍保留在 `.concurrent.*` 候选且快照完整，无数据丢失。后续建议在发布动作成功瞬间即登记 ownership。 |
| RV423-003 | P3 | OPEN | `scripts/manage.py:88-98` 在原错误为 `KeyboardInterrupt` 且自动回滚自身再次遭遇异常打断时，异常落入通用 `PackError` 导致 CLI 最终返回 1 而非 130；现场完整保留，仅影响中断退出码语义一致性。后续建议优先重抛 `original_error`。 |

## Agent Links
none — no agent contract changes

## Improvements
| improvement-id | user-value | evidence-ref |
|---|---|---|
| IMP-01 | 修复 Windows 及新版 ZCode 安装阻塞：兼容 `provider_config.json` 布局，解决初装 `model_inventory.py` 正常退出但返回空 `providers` 导致自适配中断的问题，且全流程严格脱敏，不泄露 API Key、token 或 baseURL。 | EV-07 |
| IMP-02 | 两阶段计划确认与原子防护：正式 install/update/rollback 引入 `PLAN_DIGEST` 确认机制与 OS 目录级排他锁，杜绝并发覆写与状态脏写；冲突与恢复生成唯一时间戳候选，全面保护用户本地内容。 | EV-05 |
| IMP-03 | 增强型快照与回滚安全：升级 snapshot 为 schema v2，强制绑定 agent/state 内容 SHA 并向后兼容 v1；访问过程全面拒绝 symlink 与特殊文件，失败或 Ctrl-C 触发受控回滚并干净退出。 | EV-02 |
| IMP-04 | 消除 pre-push 钩子环境污染与分支偏移风险：pre-push 钩子与测试夹具子进程全面隔离清理 GIT_* 环境变量，保障维护者发版测试与门禁检查安全幂等运行。 | EV-09 |

## Blockers
none

## Unverified
none

## Migration
breaking-impact: breaking
ordinary-users: 普通用户通过 README/INSTALL 的 v4.2.3 提示词执行安装、更新或卸载；执行流程新增只读盘点与 PLAN_DIGEST 两阶段显式确认，state schema v2 兼容读取旧版 v1。
maintainers: 维护者必须核验包含 13 项变更文件的 staged payload，确保 release gate 审查与分支 PR required checks 完整通过后合并，并在发布后推送对应标签。
upgrade: 在 ZCode 中发送 README 的 v4.2.3 提示词或执行 python3 scripts/manage.py update；涉及状态变更时先执行 dry-run 获取 PLAN_DIGEST 并通过 --confirm-plan 确认执行。

## Hand-off
| owner | action | status |
|---|---|---|
| release maintainer | 将本报告写入 `release-audits/v4.2.3.md`，单独暂存后运行 `scripts/release_gate.py check`，提交功能分支 PR，等待 3 个 required checks 通过后合入 main 并发布 v4.2.3。 | READY |
| CI runner | 运行 PR 矩阵校验 `validate (ubuntu-latest, 3.9)`、`validate (macos-latest, 3.9)`、`validate (windows-latest, 3.9)` 并通过 Windows 真机动态检查。 | READY |
| package users | 发送 README v4.2.3 更新提示词，遵循两阶段 PLAN_DIGEST 确认升级。 | READY |
| follow-up maintainer | 跟进处理非阻断项 RV423-001（force 孤儿文件）、RV423-002（回滚登记窗口）与 RV423-003（回滚中断退出码）。 | READY |
