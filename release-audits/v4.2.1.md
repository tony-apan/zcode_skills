---
report-id: RG-V421-20260911-01
role: github
reviewer: github
mode: RELEASE_GATE
version: 4.2.1
package_fingerprint: 9977f6f30fe56f3dcf1a5171328c6efcbf6b04f23e1c278935adefe141ac321e
base_ref: v4.2.0
target_ref: WORKTREE:9977f6f30fe56f3dcf1a5171328c6efcbf6b04f23e1c278935adefe141ac321e
changed_files: [".github/workflows/validate.yml", ".zcode-plugin/plugin.json", "CHANGELOG.md", "INSTALL-FOR-AI.md", "README.md", "tests/test_manage.py"]
removed_files: []
changed_agents: []
breaking_impact: none
reviewed_at: 2026-09-11T12:27:12Z
verdict: PASS
---

# Release Gate v4.2.1

## Scope

- changed_files: `.github/workflows/validate.yml`
- changed_files: `.zcode-plugin/plugin.json`
- changed_files: `CHANGELOG.md`
- changed_files: `INSTALL-FOR-AI.md`
- changed_files: `README.md`
- changed_files: `tests/test_manage.py`
- removed_files: none
- changed_agents: none

## Evidence

| evidence-id | check | result | evidence |
|---|---|---|---|
| EV-01 | execution-provenance | ATTESTED | 本报告由临时承担 `github` MODE=RELEASE_GATE 的兜底执行者出具：原 `github` 岗 provider 连续被拒，本次由兜底角色执行。执行边界为只读——仅读 `/Users/tony/Work/01_Data/010_zcode_skills` 内普通文本文件，未修改任何文件、未运行仓库脚本、未安装依赖、未接触 token 或凭据、未读取 `.git` 对象库内容。 |
| EV-02 | validation | PASS | 调用方声明 `python scripts/manage.py validate` → `VALIDATION OK: 22 agents, plugin tony-agents-pack v4.2.1`。只读复核独立确认：`scripts/manage.py` 第 24 行 `EXPECTED_AGENT_COUNT = 22`，`agents/` 目录恰为 22 个 `*.md`；`.zcode-plugin/plugin.json` 为 `name=tony-agents-pack`、`version=4.2.1`、`agents=agents`；`validate_package` 全部硬约束（40 位 SHA、`fetch-depth: 0`、checksum 含 `docs` 与 `MODEL_SETUP.md`）在 workflow 文本中逐条满足。 |
| EV-03 | tests | PASS | 调用方声明 `unittest` → `Ran 137 tests ... OK`。只读复核独立统计 `def test_`：`tests/test_manage.py` 102 处、`tests/test_release_gate.py` 29 处、`tests/test_model_inventory.py` 6 处，合计 137，与声明一致；新增/更新断言 `test_workflow_avoids_duplicate_release_runs`、`test_release_version_is_v4_2_1`、`test_readme_marks_updated_agents_and_three_tier_round_limits` 均已落盘。 |
| EV-04 | fingerprint | PASS | 独立按 `release_gate.py` 算法（index mode/blob 内容，排除版本审计报告与 `__pycache__`/`*.pyc`/根 `SHA256SUMS`/`.tony-agents-pack*`）对发布 payload 全量重算，得 `9977f6f30fe56f3dcf1a5171328c6efcbf6b04f23e1c278935adefe141ac321e`，与 `package_fingerprint` 及 `target_ref` 尾值精确一致；payload 计 47 个非审计路径。 |
| EV-05 | workflow-hard-constraints | PASS | `.github/workflows/validate.yml`：`checkout@11d5960a326750d5838078e36cf38b85af677262 # v4`、`setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5`、`upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4` 均为 40 位十六进制且带注释；两个 checkout 均 `fetch-depth: 0`；全文无 `actions/...@v<digits>` 浮动标签；checksum 步骤含 `find agents scripts tests .githooks release-audits docs` 与 `MODEL_SETUP.md`；顶层 `permissions: contents: read`。满足 `scripts/manage.py` 第 607-620 行全部 workflow 断言。 |
| EV-06 | required-check-names | PASS | `validate` 为 `matrix.os: [ubuntu-latest, macos-latest, windows-latest]` × `python-version: ['3.9']`，PR 事件下产生 `validate (ubuntu-latest, 3.9)`、`validate (macos-latest, 3.9)`、`validate (windows-latest, 3.9)` 三个名称，与分支保护 required checks 一致；新增 `validate-tag` 为无矩阵单 job，check 名为 `validate-tag`，不产生同名 required check，且其 `if` 仅在 `refs/tags/v*` 为真。 |
| EV-07 | coverage-completeness | PASS | PR 路径保留完整三平台矩阵，shell 包装语法检查（`sh -n`）与 shell 安装器 dry-run 在非 Windows runner 执行、PowerShell 安装器 dry-run 在 Windows 执行，macOS/Windows 与两个安装器均在合并前覆盖；tag 路径仅 Linux 且已由触发条件限定，其 commit 树在对应 PR 阶段已被同一矩阵完整验证。标签路径无未覆盖的独立契约面。 |
| EV-08 | concurrency-safety | PASS | `concurrency.group: ${{ github.workflow }}-${{ github.ref }}`，`cancel-in-progress: ${{ github.event_name == 'pull_request' }}`。tag push 事件 `github.event_name` 为 `push`，表达式为假，标签校验不会被并发取消；仅 PR 运行可被同 ref 的新运行取消。 |
| EV-09 | version-consistency | PASS | `plugin.json`=`4.2.1`；`CHANGELOG.md` 顶层 `## [4.2.1] — 2026-09-11`；README 徽章 `version-v4.2.1` 与 `releases/tag/v4.2.1`、安装/更新/卸载提示词与手工脚本 `tag/--branch v4.2.1`；`INSTALL-FOR-AI.md` 全篇 `v4.2.1` 与 `同为 4.2.1`；`tests/test_manage.py` 断言 `4.2.1`。残留 `4.2.0` 仅出现于 CHANGELOG 历史段、README「功能增量与 v4.2.0 相同」叙述与 `release-audits/`，均为正当历史/比较表述。 |
| EV-10 | agent-count-and-package | PASS | `agents/` 恰 22 个文件；README 含 `badge/agents-22-`、`#22-个岗位`、`## 22 个岗位` 且无 21 计数残留；`manage.py` 发布必需文件清单在树内逐一存在（含 4 张模型截图与二维码 PNG、`scripts/install.ps1`、`release-audits/README.md`、三个测试文件）。 |
| EV-11 | changed-set-and-impact | PASS | 相对 `base_ref=v4.2.0` 的 staged 差异精确为上述 6 文件，与 `changed_files` 逐项相等；无删除项，`removed_files: []` 正确；6 文件中无 `agents/*.md`，`changed_agents: []` 正确。改动面仅 CI 触发、版本号、CHANGELOG、文档措辞与测试，未触及任何 agent 必填输入/输出、工具权限或安装/更新/卸载契约，`breaking_impact: none` 正确。 |
| EV-12 | readme-ci-wording | PASS | README 第 254 行已改为「CI 在 PR 与发布 tag 上强制检查（`main` 推送不再单独触发，因为合并后的树已由该 PR 完整验证）」，与删除 `push: branches: [main]` 后的实际触发一致；检索「同时对 push」「push、PR 和 tag」均无命中，上一轮文档矛盾已消除且无新残留矛盾；新增「变更分级与审查强度」表明确 `release_gate.py check` 与 `github` 审计在任何档位都是硬门，未放宽机器门禁。 |

## Findings

| finding-id | severity | status | summary |
|---|---|---|---|
| RG-001 | P3 | OPEN | README 流水线图仍保留 `draft[内容成稿 writer] --> gonghao` 与 `draft --> review`，但 `agents/gonghao.md` 未声明 writer 为上游输入（sheyun 同样由 `lead` 驱动、契约亦未声明该上游）。该图为高层示意，易被误读为 gonghao 需 writer 先出稿；为上一轮沿用项，本轮 README 变更未触碰该图，不影响门禁行为与包内契约一致性。 |

## Agent Links

none — no agent contract changes

## Improvements

| improvement-id | user-value | evidence-ref |
|---|---|---|
| IMP-01 | 发布流水线更快更省：删去 `main` 推送的重复校验、tag 只跑 Linux、PR 启用并发取消，一次发布从 9 个 job 降为 4 个，维护者等待时间与 CI 消耗同步下降，且 PR 侧三平台矩阵与 `release_gate.py check` 硬门未放宽。 | EV-05 |
| IMP-02 | 文档与实际触发一致：README 的 CI 覆盖表述改述为「PR 与发布 tag 上强制检查」，消除上一轮「CI 同时对 push、PR 和 tag 强制检查」与删除 `push: main` 的矛盾。 | EV-12 |
| IMP-03 | 审查强度可按风险选择：新增「变更分级与审查强度」表，契约变更走双路对抗审查、非契约可走单路、PATCH 可仅做 validate/测试/审计，同时硬性保留 gate 与 github 审计。 | EV-12 |
| IMP-04 | 版本与岗位计数全链路一致（4.2.1 与 22 岗），安装、更新、卸载提示词、手工脚本与测试断言同步，普通用户升级不漂移。 | EV-09 |

## Blockers

none

## Unverified

none

## Migration

breaking-impact: none
ordinary-users: 无需任何操作即可保持现状；如需跟随版本，按 README/INSTALL 的 v4.2.1 更新提示词执行 update 即可，安装 state schema、agent 契约与模型映射保留逻辑均不变。
maintainers: 需 stage 全部 6 个发布变更，把本报告原样写入 release-audits/v4.2.1.md 后单独 git add 该审计文件，再运行 python3 scripts/release_gate.py check；此后在功能分支提交 PR，等待三个 required checks 通过并解决全部对话后 merge main。
upgrade: 在 ZCode 新会话发送 README 的 v4.2.1 更新提示词，或执行 python3 scripts/manage.py update；state 已是 4.2.1 时会报告已安装，仅在重新分配模型时才传 model-map。

## Hand-off

| owner | action | status |
|---|---|---|
| release maintainer | 把本报告原样写入 release-audits/v4.2.1.md，单独 git add 该审计文件后运行 python3 scripts/release_gate.py check，再在功能分支提交 PR 并推送 v4.2.1 tag。 | READY |
| main AI | 登记 RG-001 为后续非阻断跟进：在 agents/gonghao.md 契约或 README 流水线图中对齐 draft 到 gonghao 的上游关系。 | READY |
| package users | 按 README v4.2.1 提示词 update 即可，agent 契约、state schema 与既有调用方式均不变。 | READY |
| CI operators | 保持分支保护 required checks 为 validate (ubuntu-latest, 3.9) / validate (macos-latest, 3.9) / validate (windows-latest, 3.9)，确保 validate-tag 未被加入 required 列表。 | READY |
